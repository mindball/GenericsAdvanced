# EventArg
```
Problem with EvenArgs is that makes a very difficult to a pass along any addition informations, because is not set
in our case a property. If you  need information on which property has been changed, an additional class must be created
and inherite a EventArgs
```